services:
  - type: web
    name: pokedex-vue
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run serve
    routes:
      - source: /
        destination: /public/index.html
